import 'package:flutter/material.dart';

class HunterChatWidget extends StatelessWidget {
  const HunterChatWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
